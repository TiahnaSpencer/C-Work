#include <iostream>
#include <vector>
#include <string>
#include <fstream>
using namespace std;

vector<string>attendance = {"envy ", "ilyas ", "damon ", "hamir " ,"ajay ", "bode ", "george ", "jordan ", "chris ", "caleb ", "seth ", "ryan ", "tiahna ", "bailin ", "elijah ", "connor "};

int main() {
  string input;
  int answer;
  bool validInput;

  for (int i = 0; i < attendance.size(); i++) {
    validInput = false;
    
    while (!validInput) {
      cout << "Is " << attendance[i] << "here? (1 for yes, 0 for no): ";
      cin >> input;
      
      // Check if input is exactly one character and is either '0' or '1'
      if (input.length() == 1 && (input[0] == '0' || input[0] == '1')) {
        answer = stoi(input);
        validInput = true;
      } else {
        cout << "Invalid input. Please enter 1 (yes) or 0 (no).\n";
      }
    }
    
    if (answer == 1) {
      cout << attendance[i] << "is present.\n\n";
    } else {
      cout << attendance[i] << "is absent.\n\n";
    }
  }
  
  return 0;
}
